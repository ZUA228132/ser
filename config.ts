// The unique ID for the cloud JSON storage bin.
// This allows the download links to be persistent and shared across all users.
export const JSON_BIN_ID = '1269399852233818112';

// The full API URL for reading and writing the links data.
export const JSON_BIN_API_URL = `https://jsonblob.com/api/jsonBlob/${JSON_BIN_ID}`;
